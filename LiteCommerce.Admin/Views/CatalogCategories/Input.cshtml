@model LiteCommerce.DomainModels.Categorie
@{

}
<div class="box box-success">
    <div class="box-header with-border">
        <h5 class="box-title pull-right">
            <a href="~/catalogcustomers">
                back
            </a>
        </h5>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <form role="form">
            <!-- text input -->
            <!-- textarea -->
            <!-- input states -->
            @if (Model.CategoryID == 0)
            {

            }
            else
            {
            <div class="form-group ">
                <label class="control-label" for="inputSuccess"><i class="fa fa-fw fa-calendar-plus-o"></i> CategorieID</label>
                <input type="text" class="form-control" id="inputSuccess" placeholder="CategorieID ..." value="@Model.CategoryID">
            </div>
            }

            <div class="form-group ">
                <label class="control-label" for="inputSuccess"><i class="fa fa-fw fa-calendar-plus-o"></i> Company Name</label>
                <input type="text" class="form-control" id="inputSuccess" placeholder="Company Name ..." value="@Model.CategoryName">
            </div>
            <div class="form-group ">
                <label class="control-label" for="inputError">
                    <i class="fa fa-fw fa-list-alt"></i> Description
                </label>
                <input type="text" class="form-control" id="inputError" placeholder="Description ..." value="@Model.Description">

            </div>



            <!-- checkbox -->
            <!-- radio -->
            <!-- select -->
            <!-- Select multiple-->

            <button type="submit" class="btn btn-default pull-left">Save</button>
            <button type="submit" class="btn btn-default pull-left">Cancel</button>
        </form>
    </div>
    <!-- /.box-body -->
</div>
